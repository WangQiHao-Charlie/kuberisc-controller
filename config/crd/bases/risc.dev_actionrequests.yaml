---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: actionrequests.risc.dev
spec:
  group: risc.dev
  names:
    kind: ActionRequest
    listKind: ActionRequestList
    plural: actionrequests
    shortNames:
    - ar
    singular: actionrequest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              instructionRef:
                properties:
                  instruction:
                    type: string
                  name:
                    type: string
                  runtime:
                    type: string
                required:
                - instruction
                - name
                - runtime
                type: object
              params:
                additionalProperties:
                  type: string
                type: object
              selector:
                properties:
                  podSelector:
                    additionalProperties:
                      type: string
                    type: object
                type: object
              strategy:
                properties:
                  completionPolicy:
                    properties:
                      atLeastN:
                        type: integer
                      type:
                        type: string
                    type: object
                  parallelism:
                    type: integer
                  retryLimit:
                    type: integer
                type: object
              timeoutSeconds:
                format: int32
                type: integer
              ttlSecondsAfterFinished:
                format: int32
                type: integer
            required:
            - instructionRef
            - selector
            type: object
          status:
            properties:
              counts:
                properties:
                  failed:
                    type: integer
                  running:
                    type: integer
                  succeeded:
                    type: integer
                  total:
                    description: Split fields so each has a distinct JSON tag
                    type: integer
                type: object
              phase:
                type: string
              reason:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
